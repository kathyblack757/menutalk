# 菜根谭 MenuTalk — 执行步骤

## 开发总览

P0~P5 前端开发全部完成。下一步：后端搭建。

---

## 第0步：项目文档与基础设施搭建 ✅

## 第1步：前端项目初始化 + 清理 ✅

P0~P3 + P5 全部完成：
- 低代码平台残留清理
- 项目配置（API代理、HMR、env）
- 冗余依赖和组件清理（node_modules 515→422 包）
- 国际化文案抽离（中/英双语）
- 进度条动态计算、字体回退

## 第2步：功能开发 ✅

P4 全部完成：
- 拍照流程（CameraTab → ViewfinderPage → Loading → DishSwiper）
- 实时摄像头取景框（镂空效果 + 自适应适配）
- 点单文案（购物车生成 + 双语展示 + TTS朗读）
- 历史记录（localStorage + 列表/详情 + 购物车标记）
- 个人中心（头像上传、用户名、辣度耐受）
- 深浅色模式切换（全应用适配）
- 数据持久化（购物车 + 设置）

---

## 第3步：后端项目初始化 ⬅️ 下一步

**目标**：搭建Express服务

- Express 项目结构
- CORS + 文件上传中间件
- 健康检查接口

## 第4步：后端API开发

- OpenAI API 封装
- `POST /api/ocr` 菜品识别（含 AI推荐标记 isRecommended）
- `POST /api/dish` 菜品详情
- `GET /api/exchange` 汇率查询（实时汇率，非mock固定值）
- 菜单货币识别：AI检测货币符号 → 未检测到则用IP定位兜底
- `POST /api/order` 点单文案（AI生成多语言话术）

## 第5步：前后端对接

- 前端替换 mockDishes 为真实 API 调用
- 图片URL替换
- 端到端测试

## 第6步：UI打磨 + 响应式测试

- 手机端完整走通全流程
- 过渡动画优化
- 错误状态处理
